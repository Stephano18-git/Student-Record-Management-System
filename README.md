# 🎓 Student Record Management System (Python + Excel)

This is a simple **Student Record Management System** built with Python and Excel.  
It allows users to **add, view, edit, and delete student records** easily.

---

## 🧠 Features
- Add new student records  
- View all records  
- Edit existing records  
- Delete records  
- Data stored in an Excel file using `openpyxl`

---

## ⚙️ Technologies Used
- Python 3
- OpenPyXL library
- Excel (for data storage)

---

## 🚀 How to Run
1. Install dependencies:
   ```bash
   pip install openpyxl
